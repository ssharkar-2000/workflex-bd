DATABASE_URL="postgresql://workflex:workflex_password@localhost:5432/workflex?schema=public"
JWT_ACCESS_SECRET="change-me-access"
JWT_REFRESH_SECRET="change-me-refresh"
JWT_ACCESS_TTL="15m"
PORT=3000

# Optional — only needed for the maintenance-window email notifications.
# Without these set, MailService logs a warning and skips sending instead of
# crashing, so everything else keeps working fine.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="WorkFlex BD <no-reply@workflexbd.com>"
