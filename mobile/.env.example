# Leave this commented out — the app now auto-detects the backend's LAN IP
# from the Expo dev server connection, so it keeps working whether your
# phone is on WiFi or tethered to a mobile hotspot. Only set this if you
# need to point at a fixed URL (e.g. a deployed/tunnel backend).
# EXPO_PUBLIC_API_URL=http://10.20.211.146:3000/api
